-- ============================================================
-- ⚠️ 已废弃（勿执行）
-- 原「成员管理二级分组」方案因后台前端菜单仅两层结构会导致点击无页面，已回退。
-- 请勿再执行本脚本；已执行的库请执行 upgrade_056_menu_revert.sql 回退。
-- ============================================================
SELECT 'upgrade_055 is deprecated, use upgrade_056_menu_revert.sql instead' AS notice;
